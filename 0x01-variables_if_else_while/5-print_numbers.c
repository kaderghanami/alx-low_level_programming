#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

	for (n = 0; n < 10; n++)
	{
		putchar ('0' + n);
	}
putchar ('\n');
return (0);
}
